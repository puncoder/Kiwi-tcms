MUST be done...

1. Automated must be manual.
2. Sortkey must be given for each test case.
3. kiwi test case summary must be exact same as of robot file's test case.
4. plan name must be the robot file name without .robot extension.
5. map the plan name ( robot file name ) and its kiwi plan id inside test_plan.py file.



====================
installation ::

run time status change funtions :
/Users/plivo/PycharmProjects/Kiwi/tcms/testcases/models.py

functions : get_is_automated_status
get_is_automated
get_choiced
__str__


        import os
        import inspect
        os.system('open http://www.fakeurl.in/model/' + str(inspect.stack()[0][3]))
==============================
commands
==============================

git clone https://github.com/kiwitcms/Kiwi.git
create Db in postgres
change Db settings engine in file /tcms/settings/devel.py and /tcms/settings/common.py
    engine >> django.db.backends.postgresql

pg_hba.conf file (/etc/postgresql/9.1/main/pg_hba.conf). This line:

local   all             postgres                                peer
Should be

local   all             postgres                                md5
restart Db >> sudo service postgresql restart


export LC_ALL="en_US.UTF-8"
export LC_CTYPE="en_US.UTF-8"

virtualenv --python=python3.6 ~/virtualenvs/kiwi
. ~/virtualenvs/kiwi/bin/activate
cd Kiwi
sudo pip install -r requirements/mysql.txt
sudo pip install -r requirements/devel.txt
sudo pip install -r requirements/postgres.txt
npm install
./manage.py migrate
./manage.py createsuperuser
./manage.py runserver

Now, open http://127.0.0.1:8000/ and should be presented with your brand new Kiwi TCMS homepage!

=========================
Configuration

To be able to contact the Kiwi TCMS server
a minimal configuration file ~/.tcms.conf has to be
provided in the user home directory:

[tcms]
url = https://tcms.server/xml-rpc/
username = your-username
password = your-password
===========================


postgres query
===============

http://suite.opengeo.org/docs/latest/dataadmin/pgGettingStarted/firstconnect.html

connect Db on terminal:
    sudo -u postgres psql kiwi

all plans:
    ﻿SELECT * FROM public.test_plans;


For Plivo TCMS , read the plivo_readme.rst
==========================================
The modifications are done for Plivo TCMS in this repo by : 
        Amit Yadav Coder-AMiT 
